<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
        }

        .container {
            max-width: 500px;
            margin: 50px auto;
            padding: 20px;
        
            box-shadow: 0px 0px 5px 0px rgba(0, 0, 0, 0.916);
            border: 1px solid #ddd; /* Add border */
            border-radius: 8px; /* Add border radius for rounded corners */
            border-color:black
        }

        h1 {
            text-align: center;
            color: #333;
        }

        label {
            display: block;
            margin-bottom: 5px;
            color: #333;
        }

        input[type="text"],
        input[type="email"],
        input[type="password"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ddd;
            box-sizing: border-box;
            border-radius: 4px; /* Add border radius for rounded corners */
        }

        button[type="submit"] {
            display: block;
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            background-color: #333;
            color: #ffffff;
            border: none;
            cursor: pointer;
            font-size: 16px;
            border-color: black;
            border-radius: 4px; /* Add border radius for rounded corners */
        }

        button[type="submit"]:hover {
            background-color: #555;
        }

        a {
            display: block;
            text-align: center;
            margin-bottom: 20px;
            color: #333;
            text-decoration: none;
        }

        a:hover {
            color: #555;
        }
    </style>

</head>

<body>